package SWEA_farm_2805;

import java.io.File;
import java.io.FileNotFoundException;
import java.util.Scanner;

public class Solution_01 {

	public static void main(String[] args) throws FileNotFoundException {
		File file = new File("./src/SWEA_farm_2805/input.txt");
		Scanner sc = new Scanner(file);
//		Scanner sc = new Scanner(System.in);
		
		int T = sc.nextInt(); // 테스트케이스 수
		for (int tc = 1; tc < 2; tc++) {
			int N = sc.nextInt(); // 농장 크기
			sc.nextLine();
			
			char[][] I = new char[N][N];
			for (int r = 0; r < N; r++) {
				I[r] = sc.nextLine().toCharArray();
			} // for : 농장 생성
			
			// 좌우 (열)
			int[] dc = {-1, 1};
			
			int num = 0;
			int R = 0;
			for (int r = 0; r < N; r++) {
				if (r <= N/2) {
					num++;
					for (int d = 0; d < dc.length; d++) {
//						System.out.println("(" + r + "," + N/2 + ")");
//						R += I[r][N/2] - '0';
//						System.out.println("+" + R);
						for (int n = 0; n < num; n++) {
							int nextC = N/2 + dc[d] * n;
							R += I[r][nextC] - '0';
						}
					}
				} else {
					num--;
					for (int d = 0; d < dc.length; d++) {
						for (int n = num; n > 0; n--) {
							int nextC = N/2 + dc[d] * n;
							System.out.println("(" + r + "," + nextC + ")");
							R += I[r][nextC] - '0';
							System.out.println("+" + R);
						}
					}
				}
			}
			System.out.println(R);
			R -= (I[0][N/2] - '0') + (I[N-1][N/2] - '0');
			System.out.println(I[0][N/2] - '0');
			System.out.println(I[N-1][N/2] - '0');
			
			System.out.printf("#%d %d\n", tc, R);
		} // for : 테스트 결과 출력
	}

}
